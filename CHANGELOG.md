# Changelog

本文件記錄 **楓星小工具** 的版本異動。

---

## [v1.3.0.0] - 2026-08-23

### 改善
- **「打Boss」頁開啟 ⚔ 角色卡列時，視窗最小高度不再暴增**：角色卡列原本宣告支援 `heightForWidth`，這個屬性會一路往上傳到外層 `QStackedWidget`，導致連「練功頁」的整頁高度都被算進最小高度——一按 ⚔ 角色，最小高度就從 450 跳到 653，卡片下方空出一大片。改為由視窗自行依目前寬度計算角色卡列高度（`_sync_player_height()`），最小高度維持 450；只有卡片放大到真的需要折行時才往上補該多出來的高度，避免卡片被切掉。視窗變矮時改由 Boss 卡片區既有的捲軸吸收。

### 修正
- **單獨下載 exe 也能使用「輸入等級自動帶入升級經驗」**：升級經驗表 `經驗需求表(推測).csv` 已打包進 exe。原本只讀程式所在目錄，沒有該檔時 `EXP_TABLE` 為空、等級欄靜默失效，連帶「升級總時／還需」永遠顯示 `—`。現在改為先讀程式旁邊的 CSV（使用者可自行替換成別版數據），找不到才用打包進去的那份。
- **視窗在不同縮放比例的螢幕之間搬移時尺寸失控**：Windows 換 DPI 時（例 150% ↔ 100%）會要求「實體像素大小不變」，使視窗的邏輯尺寸被乘上 DPI 比例（730×480 一到 100% 螢幕變成 1095×720，卡片卻沒變大，於是留下大片空白）。新增 `DpiSizeKeeper`，在螢幕縮放比例改變後把邏輯尺寸改回原本的值；拖過螢幕交界時 `screenChanged` 會連發多次、系統又是稍後才套用放大，故以可重設的計時器等狀態安定後再還原。主視窗與血量刻度浮窗皆已套用，使用者自行調整過的尺寸也會保留。

---

## [v1.2.0.0] - 2026-08-14

### 新增
- **⚔ 角色卡**：「打Boss」模式在 Boss 卡片上方常駐顯示角色技能/狀態倒數（藍色標題，與 BOSS 技能區別）：劍士「魔法消除」（技能 CD 60 秒，但 BOSS 抵抗 80 秒，故倒數 80 秒）與「藥水CD」（預設 30 秒）。可用 Boss 工具列的 **⚔ 角色** 按鈕收合，狀態會記住。
- **按鍵配置**：每張倒數卡下方新增「按鍵配置」鈕——點一下再按下想綁的實體按鍵（F1~F12、Delete、字母數字、方向鍵等）即完成綁定，鈕上會顯示按鍵名稱；Esc 取消、右鍵清除。全域偵測（遊戲視窗聚焦時也有效、不攔截按鍵），按綁定鍵即觸發倒數（計次卡為 +1），免去滑鼠移過去點的延遲。
- **⚡ 觸發模式開關**（倒數卡）：⚡關（預設）＝READY 時按鍵才會開始倒數；⚡開＝倒數中按鍵也直接重新倒數。
- 綁定與 ⚡ 狀態存入 `maple_star_config.json`，重啟後保留；預設全部未綁定。

### 修正
- **普通/混沌龍王**：魔法無效化 / 物理無效化 冷卻由 30 秒修正為 **40 秒**。

---

## [v1.1.0.0] - 2026-08-07

### 新增
- **練功頁「等級」欄**：輸入角色等級（31–200）後，自動從 `經驗需求表(推測).csv` 查表填入升級所需經驗，無需手動輸入。
- 讀取 CSV 升級經驗表的 `_load_exp_table()` 工具函式。

### 改善
- **計時器改用高精度 deadline 計時**（`time.perf_counter()`）：技能冷卻卡片與練功倒數計時器皆改為以截止時間點計算剩餘秒數，避免定時器誤差累積，長時間使用不再偏移。
- **技能冷卻顯示改為整數秒**：卡片倒數由原本的一位小數（`12.3`）改為整數（`12`），畫面更簡潔易讀。

### 修正
- 暫停後繼續計時，剩餘秒數計算錯誤的問題。

---

## [v1.0.0.0] - 2026-08-07

### 初始版本
- 技能冷卻計時器：卡片式介面、巡迴模式、計次卡（計次技能）、卡片縮放。
- 練功經驗計算：起始/結束經驗輸入、倒數計時、效率計算（每分鐘/每小時）、升級剩餘估算。
- Boss 血量刻度表：獨立浮動視窗、拖曳縮放對齊血條、刻度標記。
- 視窗通用：置頂切換、透明度調整（背景/方塊獨立）、自由縮放、拖曳握把。
